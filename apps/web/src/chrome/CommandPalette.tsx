/**
 * Cmd-K command palette. PR #3 wires this with action registry,
 * search, and keyboard navigation. Skeleton renders nothing so the
 * shell layout is stable.
 */
export function CommandPalette() {
  return null;
}
