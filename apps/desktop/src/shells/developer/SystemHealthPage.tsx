import { Placeholder } from '../_shared/Placeholder';

export function SystemHealthPage() {
  return (
    <Placeholder
      title="System health"
      description="Sync lag, outbox depth, telemetry throughput, dropped sample counters."
      shipsIn="PR #3 — protocol bridges"
    />
  );
}
